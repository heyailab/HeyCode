// HeyCode widget 测试占位。
// M2 起将补充 model 单元测试与 widget 测试。

import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('placeholder test', (WidgetTester tester) async {
    // M1 仅验证骨架可编译，真实测试在后续里程碑补全。
    expect(true, isTrue);
  });
}
